{
  "name": "coreledger",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "setup": "npm install && npm install --prefix server && npm install --prefix client && node scripts/setup-env.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
